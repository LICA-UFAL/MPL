def get_version(version = None ) -> str:
    """ 
    Return string with formated version.
    
    Parameters
    ----------
    version : tuple or list
        Tuple of version infos.

    Returns
    ----------
    str
        Version string.
    """

    return ''.join(version)